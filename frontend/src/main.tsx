import React from 'react';
import ReactDOM from 'react-dom/client'; 
import PrincipalApp from './PrincipalApp.tsx'; 

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PrincipalApp /> 
  </React.StrictMode>
);
